# SQLab Pro 开源发布路演 PPT 内容脚本

> 全篇共 10 页，目标受众为技术负责人与一线研发人员。
> 每页包含：标题、核心文案、配图/演示区建议、视觉布局说明、讲稿旁白（Presenter Notes）。
> 建议总时长：12~15 分钟。

---

## P1 封面

**标题**：SQLab Pro

**核心文案**：
- 主 Slogan：让每一条 SQL，上线前都被认真对待
- 副标题：开源 SQL 测试工作台 · 查询执行 / 性能分析 / 规范校验，一站完成
- 角标：v0.9.2-beta · MIT License · github.com/sqlabpro

**配图/演示区建议**：深色底主视觉。背景为模糊的代码编辑器与数据库连接拓扑线条；Logo 居中偏上，Slogan 大字居中，底部一行小字放版本与开源协议徽章（build passing / license MIT / PRs welcome）。

**视觉布局**：居中构图。上 1/3 Logo 与产品名，中 1/3 Slogan，下 1/3 徽章条。

**Presenter Notes**：
开场 30 秒。大家好，今天介绍的是我们打磨了三个月的开源项目 SQLab Pro。它解决一个非常具体的问题：SQL 在上线之前，缺少一个"体检"环节。我们把这个体检做成了一个浏览器里就能用的开源工作台。

---

## P2 痛点共鸣

**标题**：这些场景，你可能很熟悉

**核心文案**（三个翻车场景，卡片式呈现）：
- 场景一：上线后接口突然变慢，排查两小时，发现是一条没走索引的 SQL 全表扫描
- 场景二：测试环境随手一条 UPDATE，忘写 WHERE，整表数据被更新
- 场景三：评审时同事说"SELECT * 不规范"，但没人说得清全团队有多少条这样的 SQL
- 小结句：SQL 的问题不是"不会写"，而是"上线前没有低成本验证的地方"

**配图/演示区建议**：三张翻车场景插画卡片，每张配一个红色报警图标（慢查询曲线、丢失的 WHERE、满天飞的星号），底部放小结句横条。

**视觉布局**：左文右图。左侧 1/3 放标题与引导语，右侧 2/3 三张卡片纵向排列。

**Presenter Notes**：
这一页请大家对号入座。三个场景分别对应性能、安全、规范三类事故，共同点是：发现的时候都已经晚了。如果有一条 SQL 能在提交前 30 秒内完成一次"体检"，大部分事故根本不会发生。这就是我们做 SQLab Pro 的出发点。

---

## P3 解决方案

**标题**：SQLab Pro：一个页面，覆盖 SQL 上线前三件事

**核心文案**：
- 写完就测：内置编辑器，Ctrl+Enter 直接执行，只读连接保安全
- 慢了就看：EXPLAIN ANALYZE 可视化，全表扫描一眼定位
- 不合规范就拦：内置规则引擎，SELECT *、无 WHERE 自动检出
- 架构定位一句话：纯浏览器端原型 + 轻量后端服务，Docker 一行命令拉起

**配图/演示区建议**：产品信息架构图：顶部是 SQLab Pro 工作台，向下分三个模块卡片（SQL 查询 / 性能分析 / 规范检查），底部是 PostgreSQL 数据库图标，中间用"只读连接"标注连线。

**视觉布局**：上图下文。上部 2/3 放架构示意，下部 1/3 放三行要点文字。

**Presenter Notes**：
我们的方案不是再造一个数据库客户端，而是专注"上线前验证"这一个动作。左侧写 SQL，右边立即得到三份反馈：执行结果、执行计划、规范报告。注意底部这个只读连接设计，从架构层就杜绝了误写数据。

---

## P4 功能演示一：SQL 查询

**标题**：查询编辑器：写完即测，结果一目了然

**核心文案**：
- 语法高亮 + 行号 + 快捷键 Ctrl+Enter，零学习成本
- 50 行结果秒级返回，分页浏览，支持一键导出 CSV
- 每次执行自动记录耗时与行数，沉淀到历史记录

**配图/演示区建议**：现场演示或截图。左半区是深色 SQL 编辑器（展示高亮效果与 Ctrl+Enter 提示），右半区是结果表格（ID/姓名/部门/操作时间四列，带分页器），右上角"导出 CSV"按钮用高亮框圈出。

**视觉布局**：左文右图。左侧窄栏列三个要点，右侧宽栏放产品截图/演示窗口。

**Presenter Notes**：
第一眼它像个普通查询工具，但请注意两个细节：一是执行期间按钮自动进入 Loading 态防止重复提交；二是每一次执行都会带着行数和耗时进入历史记录，这两个数据后面排查问题时非常关键。

---

## P5 功能演示二：性能分析

**标题**：执行计划可视化：让全表扫描无处可藏

**核心文案**：
- EXPLAIN ANALYZE 结果翻译成人话：哪个节点慢、慢在哪、贵多少
- 三层计划节点流 + 成本占比条，Seq Scan 一眼被标红
- 慢查询趋势图：P95 耗时柱状 + 执行次数折线，300ms 阈值线直观告警

**配图/演示区建议**：现场演示或截图。上半部分为计划节点流（Limit → Gather Merge → Seq Scan，第三节点黄色警示样式，成本进度条接近满格）；下半部分为 24 小时慢查询 ECharts 图表，红色虚线阈值线清晰可见。

**视觉布局**：上图下文。上部 2/3 放性能分析页截图，下部 1/3 放两行结论文字。

**Presenter Notes**：
这是我个人最喜欢的功能。原生 EXPLAIN 输出对新人非常不友好，我们把它渲染成节点流，每个节点标注 cost、耗时、行数。演示里这条 SQL 的瓶颈一眼可见：Seq Scan 扫了 98,420 行，占总成本 92%。配套的慢查询图回答另一个问题：这个慢是偶发还是常态。

---

## P6 功能演示三：规范检查

**标题**：规则引擎：把团队 SQL 规范变成自动检查

**核心文案**：
- Error / Warning / Info 三级结果，每条附定位（行/列）与修复建议代码
- 默认 5 条规则：禁止 SELECT *、必须有 WHERE、LIMIT 保护、索引提示、命名风格
- 编辑器内容实时同步，检查零拷贝；支持按团队扩展自定义规则

**配图/演示区建议**：现场演示或截图。左侧为问题样例 SQL（SELECT * 版本），右侧为检查结果列表：红色 Error 卡片、黄色 Warning 卡片、蓝色 Info 卡片各一条，每条卡片内的绿色"建议修复"代码块用放大镜效果突出。

**视觉布局**：左文右图。左侧窄栏标题与要点，右侧宽栏放检查页截图。

**Presenter Notes**：
规则的真正价值在团队层面：新人不需要背规范，提交前跑一次检查就知道。演示的这条 SQL 命中了 4 条问题，包括两条 Error 级。每条建议都给了可直接参考的修复代码，把"评审时的口头提醒"变成"工具里的自动交付"。

---

## P7 技术亮点

**标题**：为工程可靠性而设计

**核心文案**：
- 只读事务 + 风险语句拦截，架构层防误写（UPDATE 无 WHERE 直接拒绝）
- 规则引擎基于 SQL 解析树而非正则，误报率低，可按团队扩展
- 关键指标可观测：执行耗时、扫描行数、计划成本全程留痕
- 纯前端原型零依赖可运行，后端 Docker 一行命令部署

**配图/演示区建议**：中心放射图：中心是 SQLab Pro Logo，四个方向各放一张特性徽章卡（Security / Rules Engine / Observability / Easy Deploy），每张卡配一个小图标（盾牌、分支树、仪表盘、鲸鱼）。

**视觉布局**：上标题下图形，四卡 2x2 网格排列，中心 Logo 汇聚。

**Presenter Notes**：
给技术负责人重点讲两点。第一，安全性不是靠提示语，而是只读事务这种架构层约束；第二，规则引擎走的是解析树，团队可以把自有规范沉淀成规则文件，这正是它作为开源项目最有生命力的部分。

---

## P8 快速上手

**标题**：三步用起来

**核心文案**：
- 第一步：docker run 一行命令启动服务
- 第二步：浏览器打开工作台，填入测试库连接（只读账号）
- 第三步：粘贴第一条 SQL，Ctrl+Enter，看结果、看计划、看检查
- 提示：建议先从测试/预发环境接入，生产使用只读账号

**配图/演示区建议**：左侧为终端风格代码块（docker run 命令，逐行高亮），右侧为三步流程条（1 启动 → 2 连接 → 3 体检），每步配小图标。

**视觉布局**：左文右图。左侧代码块占 1/2，右侧步骤条纵向排列占 1/2。

**Presenter Notes**：
我们刻意把接入成本压到最低：不装客户端、不改应用代码，Docker 一行命令，五分钟内可以跑通第一条 SQL。散会后大家可以扫码进仓库，README 里就是这一页的三步。

---

## P9 Roadmap

**标题**：接下来我们要做什么

**核心文案**：
- v1.0：自定义规则集 + 规则导入导出（团队规范一键共享）
- v1.1：MySQL 8 支持、执行计划优化前后 Diff 对比
- v1.2：团队工作区、SQL 评审链接分享
- 长期方向：成为 SQL 质量门禁的事实标准组件，接入 CI 流水线

**配图/演示区建议**：横向时间轴，从左到右四个里程碑节点，节点上方贴版本号徽章，下方一行说明；时间轴尾部用虚线箭头指向"CI 门禁"目标插画。

**视觉布局**：上图下文。中部横贯时间轴图形，底部一行"长期方向"文字条。

**Presenter Notes**：
Roadmap 按使用深度排期：先让规则能共享，再覆盖更多数据库，然后进入协作场景。最重要的是最后这个方向：把 SQL 体检做成 CI 里的质量门禁，问题在流水线就被拦下，而不是在凌晨三点的事故群里。

---

## P10 Q&A / 开源地址

**标题**：加入我们，一起把 SQL 体检做好

**核心文案**：
- 开源地址：github.com/sqlabpro/sqlab-pro
- 文档：sqlabpro.dev/docs · 快速上手五分钟
- 我们在找：规则贡献者、数据库方言适配者、真实场景反馈
- 大字：Q&A · 欢迎在 issue 里砸需求

**配图/演示区建议**：深色底收尾页。居中大号 Q&A 字样，下方二维码（仓库地址）+ 二维码（文档站），底部一排社区徽章（issues / discussions / contributors welcome）。

**视觉布局**：居中构图，与 P1 呼应。上半 Q&A 大字，中部双二维码并排，底部徽章条。

**Presenter Notes**：
收尾 30 秒。SQLab Pro 现在是 beta 阶段，最需要的是真实场景的反馈：你的团队有哪条 SQL 规范特别想自动化？你的库是 MySQL 还是 PG？欢迎开 issue 告诉我们。如果今天的演示让你想立刻试一下，扫左边这个码，五分钟就能跑起来。谢谢大家。

---

## 附录：制作说明

- 母版建议：深色科技风（#0d1117 底色），与产品原型视觉一致， accent 色取 #2f81f7 → #8957e5 渐变
- 演示截图统一使用 HTML 原型实拍，保留 Loading/Toast 状态以体现交互细节
- P4~P6 若现场允许，建议从截图切换为实时演示（演示脚本按 P4→P6 顺序准备，各 90 秒）
