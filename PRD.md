# SQLab Pro 产品需求文档（PRD）

| 文档信息 | 内容 |
| --- | --- |
| 产品名称 | SQLab Pro |
| 版本 | v0.9.2-beta |
| 文档状态 | 初稿（PRD Draft） |
| 作者 | 产品组 |
| 更新日期 | 2026-09-09 |
| 目标读者 | 研发工程师、技术负责人、DBA |

---

## 一、产品概述

SQLab Pro 是一款面向研发团队的开源 SQL 测试工作台，将 SQL 查询执行、执行计划解析与规范校验整合在同一个浏览器工作台中，帮助工程师在把 SQL 上线之前发现性能风险与规范违规，减少慢查询和线上事故。

一句话定位：**写完 SQL、跑一遍、看清楚、再上线。**

核心价值主张：

- 安全：默认只读连接与风险语句拦截，测试库上也不会误改数据。
- 透明：让每一次执行的代价（扫描行数、耗时、成本）可视化。
- 规范：把团队 SQL 规范沉淀为可执行的自动检查规则。

---

## 二、快速入门（Quick Start）

### 2.1 环境要求

- 浏览器：Chrome / Edge / Firefox 最新两个大版本
- 数据库：PostgreSQL 12 及以上（MySQL 支持在 Roadmap 中）
- 网络：能访问目标数据库实例

### 2.2 三步开始使用

```bash
# 1. 启动服务（Docker 方式）
docker run -d -p 8080:8080 \
  -e TARGET_DSN="postgresql://reader:******@10.2.1.8:5432/employee_db" \
  sqlabpro/server:v0.9.2

# 2. 打开工作台
# 浏览器访问 http://localhost:8080

# 3. 在「SQL 查询」编辑器输入第一条 SQL，按 Ctrl+Enter 执行
SELECT id, name, dept FROM employee WHERE dept = '研发部';
```

### 2.3 连接配置要点

- 首次使用请在「连接设置」中录入目标库 DSN，工作台默认以 **只读事务** 方式连接。
- 生产实例建议使用仅具备 `SELECT` 权限的账号，即使规则被绕过也无法写入数据。
- 连接状态可在左下角连接卡片中查看（延迟、版本、只读标识）。

---

## 三、功能模块详解

### 3.1 查询编辑器

查询编辑器是用户的主操作区，承担 SQL 编写、执行、结果查看与导出。

**功能清单**

| 功能 | 说明 |
| --- | --- |
| 语法高亮 | 关键字、字符串、注释、函数、数字分色显示，等宽字体 |
| 执行快捷键 | `Ctrl + Enter`（macOS 为 `Cmd + Enter`）直接执行当前 SQL |
| 多语句守卫 | 默认仅允许单条语句执行，多条语句给出提示 |
| 结果表格 | 支持分页（默认每页 10 行）、列悬停查看完整值 |
| 结果导出 | 一键导出当前结果集为 CSV（UTF-8 BOM，Excel 可直接打开） |
| SQL 格式化 | 关键字换行对齐，便于评审与分享 |
| 执行耗时展示 | 每次执行显示返回行数与毫秒级耗时 |
| 历史自动记录 | 执行成功或失败均写入「历史记录」，可一键复制 SQL |

**交互规则**

1. 执行期间按钮进入 Loading 态并禁用，防止重复提交；结果区展示骨架屏。
2. 执行失败时，错误信息以 Error Toast 呈现，并同步写入历史记录。
3. 切换数据源下拉框后，下一次执行自动路由到新连接。

### 3.2 执行计划解析

面向「这条 SQL 为什么慢」的问题，提供可视化的 EXPLAIN ANALYZE 解读。

**功能说明**

- 一键分析：编辑器右侧「性能分析」按钮将当前 SQL 送入本模块，自动执行 `EXPLAIN ANALYZE`。
- 计划可视化：将计划树渲染为自下而上的节点流，每个节点展示：
  - 节点类型（Seq Scan / Index Scan / Sort / Limit 等）
  - cost 区间、实际耗时、返回行数
  - 成本占比进度条（相对总成本归一化）
- 风险识别：对全表扫描、行数放大、缓冲区占用过高等模式给出 Warning 标注与中文解释。
- 慢查询分析面板：以最近采样为数据源，提供 P95 耗时（柱状）与执行次数（折线）的 24 小时趋势图，内置 300ms 阈值线，支持 1 小时 / 24 小时 / 7 天切换。
- 指标卡：总成本、扫描行数、实际耗时、缓冲区占用四项核心指标一屏总览。

**判定逻辑（示意）**

| 现象 | 阈值 | 结论 |
| --- | --- | --- |
| Seq Scan 行数 > 10,000 | 单节点 | 标记性能风险，建议评估索引 |
| 实际耗时 > 300ms | 单条 SQL | 进入慢查询统计口径 |
| 缓冲区占用 > 16MB | 单节点 | 提示 I/O 压力 |

### 3.3 规则校验引擎

把团队 SQL 规范固化为规则引擎，执行前即可发现违规写法。

**规则校验结果分级**

- Error：阻断级问题，强烈建议修复后再执行（红色）
- Warning：潜在风险，需要人工确认（黄色）
- Info：风格与可维护性建议（蓝色）

**默认内置 5 条规则**

| 规则 ID | 级别 | 触发条件 | 修复建议 |
| --- | --- | --- | --- |
| no-select-star | Error | 出现 `SELECT *` | 显式列出所需字段，利于覆盖索引与结构变更兼容 |
| require-where | Error | `UPDATE` / `DELETE` 无 WHERE 条件 | 补充过滤条件；只读连接下此类语句将被直接拒绝 |
| limit-rows | Warning | 无 LIMIT 且目标表行数超阈值 | 追加 `LIMIT` 或分页，避免大结果集拖垮接口 |
| index-hint | Warning | WHERE 过滤列未命中索引 | 评估创建索引，如 `CREATE INDEX idx_t_col ON t (col);` |
| naming-style | Info | 别名或字段名不符合 snake_case | 统一命名风格，降低协作成本 |

**交互说明**

1. 「规范检查」编辑器与「SQL 查询」编辑器内容实时同步，无需重复粘贴。
2. 检查结果按 Error → Warning → Info 排序，每条包含：级别、规则 ID、定位（行/列）、原因说明与修复建议代码。
3. 支持「仅看 Error」过滤；规则集页展示全部规则及其级别，后续版本支持按团队扩展自定义规则。

---

## 四、常见问题 FAQ

**Q1：SQLab Pro 会不会改动我的数据？**

不会。工作台默认以只读事务连接目标库，`UPDATE`、`DELETE`、`DDL` 等写操作会被拒绝执行（参考历史记录中的风险拦截示例）。生产环境建议进一步使用仅 `SELECT` 权限的账号接入。

**Q2：EXPLAIN ANALYZE 会影响线上性能吗？**

`EXPLAIN ANALYZE` 会真实执行 SQL。建议优先在测试或预发实例上分析；若必须在生产分析，请为 SQL 加上 `LIMIT`（分析模块会自动附带 LIMIT 保护），并避开业务高峰期。

**Q3：查询结果可以导出吗？有行数限制吗？**

可以。结果表格支持一键导出 CSV（UTF-8 BOM 编码，Excel 直接打开不乱码）。为保护数据库，单次导出上限为 10,000 行，超出时建议在 SQL 侧分页后再导出。

---

## 五、后续规划（Roadmap 摘要）

- v1.0：自定义规则集与规则导入导出
- v1.1：MySQL 8 支持、执行计划对比（优化前后 Diff）
- v1.2：团队工作区、SQL 评审链接分享

---

*本文档为 PRD 初稿，功能边界与阈值指标将随灰度反馈持续修订。*
